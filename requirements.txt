# requirements.txt — sv6 (albaran-valoracion-persist)
# El paquete común se instala aparte (trae httpx, sqlalchemy, pydantic y,
# para el worker, azure-storage-*):
#   local : pip install -e ../comun
#   docker: lo instala el Dockerfile desde el contexto de build
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
pydantic-settings>=2.2
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27
sqlalchemy>=2.0.30
