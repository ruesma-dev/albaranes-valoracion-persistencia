# requirements.txt — sv6 (cálculo y persistencia de valoración)
# Paquete común: pip install -e ../comun (local) / Dockerfile (build)
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
sqlalchemy>=2.0.30
psycopg[binary]>=3.1
pyyaml>=6.0
